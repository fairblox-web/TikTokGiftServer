from flask import Flask, request, jsonify

app = Flask(__name__)

# 🧺 เก็บข้อมูลของขวัญล่าสุดไว้ชั่วคราวในหน่วยความจำ
latest_gifts = []

# ✅ เส้นทางหลัก รับ Webhook จาก TikFinity
@app.route("/tiktok-event", methods=["POST"])
def tiktok_event():
    global latest_gifts

    # ตรวจสอบรูปแบบข้อมูลที่ส่งเข้ามา
    if request.is_json:
        data = request.get_json()
    else:
        data = request.form.to_dict()  # รองรับกรณี TikFinity ส่งแบบ form-data

    # ✅ พิมพ์ข้อมูลของขวัญที่ได้รับลงใน Log ของ Render
    print("🎁 ได้รับของขวัญ:", data)

    # ✅ เพิ่มข้อมูลใหม่เข้าไปในรายการล่าสุด
    latest_gifts.append(data)

    # ✅ แสดงรายการของขวัญทั้งหมดที่เก็บอยู่ในหน่วยความจำ
    print("🧠 ของขวัญทั้งหมดในระบบตอนนี้:", latest_gifts)

    # ตอบกลับ TikFinity เพื่อบอกว่าเซิร์ฟเวอร์ทำงานปกติ
    return jsonify({"status": "ok"})


# ✅ เส้นทางให้ Roblox ดึงข้อมูลของขวัญล่าสุด
@app.route("/get-latest-gifts", methods=["GET"])
def get_latest():
    global latest_gifts

    # ส่งข้อมูลปัจจุบันกลับไป แล้วเคลียร์ออกจากหน่วยความจำ
    gifts_to_send = latest_gifts
    latest_gifts = []

    print("📤 ส่งข้อมูลของขวัญให้ Roblox:", gifts_to_send)
    return jsonify(gifts_to_send)


# ✅ เส้นทางสำหรับล้างข้อมูลของขวัญทั้งหมด (รีเซ็ตหน่วยความจำ)
@app.route("/clear-gifts", methods=["POST"])
def clear_gifts():
    global latest_gifts
    latest_gifts = []
    print("🧹 Cleared all stored gifts.")
    return jsonify({"status": "cleared"})


# ✅ เส้นทางเริ่มต้น (ถ้าเปิดหน้าเว็บหลัก)
@app.route("/", methods=["GET"])
def home():
    return "✅ TikTok Gift Server is running!"


if __name__ == "__main__":
    app.run(host="0.0.0.0", port=3000)
